import styles from "./index.module.scss";

const Logo = () => {
  return (
    <a href="/" className={styles.link}>
      <h1 className={styles.text}>BEN STYLE</h1>
    </a>
  );
};

export default Logo;
